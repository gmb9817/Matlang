# Next Steps

> Future AI: read `docs/handoff/FUTURE_ONLY.md` first. Use this file as the broader post-plan backlog, not as the initial handoff.

The original repository plan's core bring-up is effectively complete. The items below are post-plan follow-ons focused on MATLAB compatibility depth, broader library/runtime surface area, and stronger backend/interop maturity rather than missing top-level subsystems.

1. Deepen the new bytecode backend/VM stage beyond the new verifier/artifact/bundle baseline: add explicit block/control-flow structure, stronger bytecode tooling/debugging, richer bundle manifests beyond the current explicit module-id plus source-path metadata, artifact/bundle versioning/evolution hooks, and eventually native-backend handoff so backend emission is more than a text dump plus in-process VM.
2. Deepen interop beyond the new workspace snapshot baseline: add MAT-file-compatible import/export layers, stronger type/shape metadata, and bridges that can exchange more of the eventual runtime surface than the current snapshot subset.
3. Deepen stdlib breadth on top of the new reduction/search/order/shape/sequence/text-helper/formatting baseline: add `sprintf`-style helpers, `pad` option parity, `replace`/`erase` variants with positional semantics, `linspace` option parity, `unique` option/mode variants, richer `flip`/cumulative dimension arguments, and broader multi-output conventions for helpers that now only support the default subset (`max`, `min`, `find`, `sort`, `diag`, `repmat`, `unique`, `split`, `extractBefore`, `extractAfter`, `extractBetween`, `replaceBefore`, `replaceAfter`, `replaceBetween`, `join`, `pad`, `compose`, `num2str`).
4. Deepen text/runtime behavior beyond the new baseline: keep building on the new string-array/text-matrix surface toward exact MATLAB char-vs-string semantics, a dedicated string-array/value-class model if needed, richer text indexing/assignment shapes beyond the current row-vector char subset, broader text conversion/output/search utilities, and broader command-form parsing past the current dotted/path-like/flag-style raw-argument baseline into escaping, quoting, and harder ambiguity cases.
5. Broaden runtime/index semantics beyond the current slice baseline: build on the new first-class logical values to reach more exact MATLAB logical-array behavior, then push richer `end` semantics inside more expression forms, more-than-2D indexing, and deeper MATLAB shape/expansion rules beyond the current scalar/vector/row-column delete support.
6. Deepen special storage/workspace execution beyond the current baseline: sibling nested-function sharing, transitive shared mutation across multiple nested closures, and broader cross-file/global behavior edge cases.
7. Deepen callable/runtime behavior beyond the current baseline: broader function-handle behavior, better script-local/function-file interaction, more exact MATLAB call/index ambiguity handling, richer builtin/output conventions at execution time, and deeper try/catch/error/reflection/warning semantics than the current baseline (`throw`, `rethrow`, preserved stacks, `cause`, formatted `error(...)`, formatted `MException(...)`, current `MException` helpers including `getReport(...)` basic/extended mode, the new `warning(...)` / `lastwarn(...)` shared-state baseline, and the current `class`/`isa` parity).
8. Deepen struct/runtime object behavior: build on the new struct-array plus implicit-root-growth baseline and the new first `classdef` subset toward broader field deletion/query behavior, deeper nested assignment edge cases, deeper comma-separated-list behavior beyond the current cell-content/struct-field/forwarded-field/forwarded-brace/forwarded-paren slice across assignments/calls/literal construction, richer struct-array expansion/readback beyond the current subset, richer interaction with indexing/calls across struct arrays and cell-held structs, user-defined inheritance beyond builtin `handle`, attributes/access control/static methods, object arrays, and eventual migration of more object-like compatibility beyond the current struct-backed `MException` baseline.
9. Deepen the HIR and optimizer from structural cleanup into more execution-oriented infrastructure: stable statement ids, verification, CFG/block structure, and more advanced passes than the current constant-fold/branch-prune baseline.
10. Deepen resolver precedence beyond the current scaffolding: encode more MATLAB-specific ordering and visibility rules directly in semantics/HIR, including script/function shadowing, richer path-root policies, and package/local precedence edge cases.
11. Expand parser support for more MATLAB constructs that feed semantics, HIR, and execution directly, especially fuller command-form coverage and broader ambiguity cleanup.
12. Add more fixtures around backend emission, text/command-form edge cases, try/catch edge cases, logical typing/indexing edge cases, remaining deletion edge cases, special workspace storage, package/private precedence, external handles, deeper comma-separated-list edge cases, structs, matrix ambiguities, ordering/multi-output builtin options, and interop snapshot/bundle workflows.

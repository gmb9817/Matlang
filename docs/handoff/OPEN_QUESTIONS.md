# Open Questions

1. Which native backend should be considered the primary compiler target after bytecode: LLVM IR, C emission, or another target?
2. Should the runtime use reference counting first, or a tracing collector first?
3. How strict should Release 0.1 command-form compatibility be?
4. How much of `classdef` should be admitted before Release 0.1 is cut?
5. What is the initial policy for sparse arrays: excluded entirely, or represented as a stubbed type early?

f = figure(27);
top = subplot(211);
default_view = view();
surface_handle = surf([0, 1, 0; 1, 2, 1; 0, 1, 0]);
top_view = view(2);
queried_top = view();
three_view = view(3);
queried_three = view();
custom_view = view([45, 20]);
queried_custom = view();
lower = subplot(212);
mesh([0, 1; 1, 0]);
lower_default_view = view(lower);
top_query_view = view(top);
view(top, 2);
top_view_after_target = view(top);
lower_view_after_top_target = view(lower);
view(lower, [10, 40]);
lower_view_after_target = view(lower);
view(top, 15, 25);
top_view_after_scalars = view(top);
current = gcf();

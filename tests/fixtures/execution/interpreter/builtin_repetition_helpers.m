a = repmat([1 2; 3 4], [2, 3, 2]);
b = size(a);
c = ndims(a);
d = numel(a);
e = length(a);
f = repmat({1, 2; 3, 4}, [1, 2, 2]);
g = size(f, [1, 2, 3]);
h = repmat(5, [2, 1, 3]);
i = size(h);
j = repmat([1 2; 3 4], 2);
k = repmat([1 2; 3 4], 2, 1, 2);
l = size(k);
char_rep = repmat('ab', 2, 3);
char_rep_sz = size(char_rep);
char_rep2 = repmat(reshape('ab', 2, 1), 1, 2);
char_rep2_sz = size(char_rep2);
m = repelem([1 2 3], 2);
n = repelem([1; 2; 3], [1 2 1]);
o = repelem([1 2; 3 4], 2, 3);
p = repelem({1, 2; 3, 4}, [1 2], [2 1]);
q = repelem("x", 3);
r = repelem(reshape('abcd', 2, 2), 2, 1);
s = repelem(reshape('abcd', 2, 2), 1, 2);
t = repelem(reshape('ab', 2, 1), [1 2]);

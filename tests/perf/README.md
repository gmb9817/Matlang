Use this folder for performance regression assets.

Each case should define:

- the workload
- the command or harness entry point
- the metric being compared

Avoid cases that depend on network access or unstable external state.

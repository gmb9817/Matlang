Use this folder for macrobenchmarks.

These cases should cover full flows such as:

- parse -> semantics -> IR -> execution
- workspace export/import on larger scripts
- graphics generation with realistic figure counts

Prefer scenarios that are large enough to expose regressions but still practical for local reruns.

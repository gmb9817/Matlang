Bootstrap inputs and expected outputs live here.

- `samples/`: seed programs or curated source fragments
- `fixtures/`: normalized bootstrap-ready inputs
- `golden/`: expected rendered outputs, workspaces, or artifact summaries

This folder is for staged bring-up work, not the main day-to-day fixture suites under `tests/fixtures/`.

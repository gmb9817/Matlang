# matc

Owns the user-facing command-line driver and compile/run orchestration.

Out of scope:
- core language semantics
- runtime behavior definitions

Current implemented commands:
- `matc parse <file.m>`
- `matc check <file.m>`
- `matc help`
